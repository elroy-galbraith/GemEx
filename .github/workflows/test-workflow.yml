name: Test Trading Analysis

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  test-analysis:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Create output directory
      run: |
        mkdir -p trading_session
        
    - name: Test environment variables
      run: |
        echo "Testing environment variables..."
        echo "Note: Secrets are only available in steps that explicitly use them"
        echo "These will be available in the next step when we run the analysis"
        
    - name: Run trading analysis
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        echo "🚀 Running trading analysis..."
        echo "Testing environment variables..."
        if [ -n "$GEMINI_API_KEY" ]; then
          echo "✅ GEMINI_API_KEY is set"
        else
          echo "❌ GEMINI_API_KEY is not set"
        fi
        
        if [ -n "$TELEGRAM_BOT_TOKEN" ]; then
          echo "✅ TELEGRAM_BOT_TOKEN is set"
        else
          echo "❌ TELEGRAM_BOT_TOKEN is not set"
        fi
        
        if [ -n "$TELEGRAM_CHAT_ID" ]; then
          echo "✅ TELEGRAM_CHAT_ID is set"
        else
          echo "❌ TELEGRAM_CHAT_ID is not set"
        fi
        
        echo "Starting analysis..."
        python gemex/market_planner.py
        

        
    - name: Upload results (if available)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ github.run_number }}
        path: trading_session/
        retention-days: 7
